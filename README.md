I initially created a Product class with the private attributes: ID, Name, and Price. 

I later utilized a constructor to initialize these values, however had to set it to public in order for the values to be used in the main function. 

Then I utilized a destructor to prevent possible memory leaks and to destroy the objects. 

After, I created a method printDetails() to display the product information (ID, Name, and Price). 

I utilized the main function to input information and call printDetails() to verify the information. 
