I created a program with many different functions to store, sort, and search information. 

I used binary search to allow the user to search for the name of an Item in inventory to see whether or not it exists in inventory, and if applicable, its quantity. 

Additionally, I made sure to create a function to sort the array elements, by sorting them alphabetically by item name independent of item quantity. 

I created a function to search for the item in the array elements. 

I prompted users for item details and allowed users to input their own items and their quantities. 

I also created a function to display the item name and quantities after they have input item names and quantities. 

I made sure to set some variables as constant to prevent some functions from altering array element data. 
